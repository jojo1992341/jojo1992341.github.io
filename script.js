/**
 * COACH PROGRESSION - Moteur complet (v6.0)
 * Architecture: MVC + Services + Toggle System
 */

// Initialisation de l'application (Global Scope)
document.addEventListener('DOMContentLoaded', () => {
    // AppController est maintenant attaché à window
    window.app = new window.AppController();
});